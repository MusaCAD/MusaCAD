app-id: org.musacad.MusaCAD
runtime: org.kde.Platform
runtime-version: '6.11'
sdk: org.kde.Sdk
command: musacad_app

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --share=network

modules:
  - name: musa-cad
    buildsystem: cmake-ninja
    builddir: true
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DMUSACAD_BUILD_DEV_TOOLS=OFF
    sources:
      - type: git
        url: https://github.com/MusaCAD/MusaCAD.git
        commit: fd488b64192675766ab18836a76d5f73696c66e6
        x-checker-data:
          type: git
          tag-pattern: "^v([\\d.]+)$"
