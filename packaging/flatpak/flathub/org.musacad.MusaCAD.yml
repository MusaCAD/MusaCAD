app-id: org.musacad.MusaCAD
runtime: org.kde.Platform
runtime-version: '6.11'
sdk: org.kde.Sdk
command: musacad_app

finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --share=network

modules:
  - name: musa-cad
    buildsystem: cmake-ninja
    builddir: true
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
      - -DMUSACAD_BUILD_DEV_TOOLS=OFF
    sources:
      - type: git
        url: https://github.com/MusaCAD/MusaCAD.git
        commit: d773acf3b15fbe3a4cbcaeb59dbdfc0107aff344
        x-checker-data:
          type: git
          tag-pattern: "^v([\\d.]+)$"
